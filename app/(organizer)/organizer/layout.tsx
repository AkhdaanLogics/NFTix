import OrganizerLayout from "@/app/components/organizer/OrganizerLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <OrganizerLayout>{children}</OrganizerLayout>;
}
